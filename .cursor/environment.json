{
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "start": "sudo service docker start && docker compose up -d --build",
  "ports": [
    {
      "name": "app",
      "port": 3000
    }
  ]
}
