{
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "install": "npm ci",
  "start": "sudo service docker start && docker compose up -d",
  "terminals": [
    {
      "name": "dev",
      "command": "npm run dev"
    }
  ]
}
