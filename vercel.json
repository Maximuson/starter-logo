{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "npm ci",
  "buildCommand": "prisma migrate deploy && node scripts/strip-github-only.js && next build && node scripts/restore-github-only.js"
}
